{{#each posts as |post|}}
<div class="card post">
    <div class="card-header">
        <h2>{{post.title}}</h2>
    </div>
    <div class="card-body">
        <p>{{post.body}}</p>
    </div>
</div>
{{/each}}